import 'package:flutter/material.dart';

class CustomColors {
  final Color PRIMARY_LIGHT = (Colors.green[50])!;
  final Color PRIMARY = (Colors.green[200])!;
  final Color SECONDARY = (Colors.green[300])!;
}
